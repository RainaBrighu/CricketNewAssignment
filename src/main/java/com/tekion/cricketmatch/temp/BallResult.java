package com.tekion.cricketmatch.temp;

public enum BallResult {
    /*
    DOT(0), ONE(1), TWO(2), THREE(3), FOUR(4), FIVE(5), SIX(6), WICKET(7);

    private final int value;
    BallResult(int value) {
        this.value = value;
    }
    public int getValue(){
        return this.value;
    }

    public static BallResult predictScore() {
        BallResult[] ballResult = BallResult.values();
        Random g1 = null;
        try {
            g1 = SecureRandom.getInstanceStrong();
        } catch (Exception e) {
            e.printStackTrace();
        }
        return ballResult[Objects.requireNonNull(g1).nextInt(ballResult.length)];
    }

     */
}
